# .env file content
# Replace these values with your actual PostgreSQL credentials
DB_USER=postgres
DB_PASSWORD=mohdrifat462
DB_HOST=localhost
DB_PORT=5432
DB_NAME="GTR_WEB_SQL"

# The final, structured DATABASE_URL
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}